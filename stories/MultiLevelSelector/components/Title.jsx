import styled from 'styled-components'
import { spacingUnit } from '../theme'

const Title = styled.div`
  font-weight: 900;
  padding: ${spacingUnit(2)}px 0;
  font-size: 18px;
  margin: 0 ${spacingUnit(1)}px;
  color: rgba(0, 0, 0, 0.87);
`

export default Title
